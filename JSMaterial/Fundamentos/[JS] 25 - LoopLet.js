// Loop For x Let
// Estrutura basica do loop for
// for (variável; regra fim do loop ou comparação; progressão)
// Utilizando Let a variável só fica disponível no bloco do loop

for (let i = 0; i < 10; i++)
    {
            console.log(i)
    }
//    console.log('i:', i) // Neste caso da erro, por que usando let a variável fica disponível apenas no bloco e fora do bloco i não existe
